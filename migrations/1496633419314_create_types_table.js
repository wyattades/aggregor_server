exports.up = function(pgm) {
  // let sql = `CREATE TABLE types (
  //   name char(32) PRIMARY KEY NOT NULL,
  //   data jsonp PRIMARY KEY NOT NULL,
  //   last_fetch timestamp DEFAULT now()
  // );`;

  // pgm.sql(sql);
};

exports.down = function(pgm) {
  // let sql = `DROP TABLE types;`;

  // pgm.sql(sql);
};
